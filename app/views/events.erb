<h2>Hourly Breakdown</h2>
<table class="highlight striped">
<thread>
      <tr>
        <th data-field="id">Hour</th>
        <th data-field="id">Requests</th>
      </tr> -->
      <tbody>
        <% (0..23).step do |index| %>
    <tr>
      <td><%= index.to_s.rjust(2, "0") %></td>
      <td><%= @event_name %><%= @events_by_hour[index.to_s.rjust(2, "0")] %></td>
    </tr>
          <% end %>
      </tbody>
</thread>
</table>
</div>
